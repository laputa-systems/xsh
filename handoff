Handoff is recorded in FRONTEND-CAMPAIGN.md:1186.

  For the next coding agent:

  - Indexed-only execution is implemented across production, tooling, native tests, dynamic modules, signal hooks, and auto-main.
  - Arena-oracle mode, recursive evaluator, runtime registries, compatibility adapters, and old evaluator tests are deleted.
  - Remaining Phase 6 work is the recursive builder scratch now named IrBuildExpr, IrBuildStmt, IrBuildPattern, IrBuildFunction, and related top-level objects.
    Replace it with direct indexed construction or non-recursive indexed scratch; don’t treat the rename as deletion.

  - Then run scripts/frontend-campaign-phase6, which still needs layout, coverage/stats, two bench-fast passes, and final review.
