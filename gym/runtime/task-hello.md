Create one file named answer.txt in the task working directory.

The file content must be exactly the word hello. A trailing newline is
acceptable; no other content is allowed.

The evaluator checks the file after you exit. Nothing else is required.
