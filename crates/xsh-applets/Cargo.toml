[package]
name = "xsh-applets"
version = "0.0.1"
edition = "2024"
publish = false

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
rustc-hash = "2.1.3"
rustix = { version = "1.1.4", features = ["net", "process"] }

[lints]
workspace = true
